import { StyleSheet, Text, View } from "react-native";

const depenses = () => {
  return (
    <View>
      <Text>depenses</Text>
    </View>
  );
};

export default depenses;

const styles = StyleSheet.create({});
