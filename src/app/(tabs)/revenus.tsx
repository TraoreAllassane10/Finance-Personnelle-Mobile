import { StyleSheet, Text, View } from "react-native";

const revenus = () => {
  return (
    <View>
      <Text>revenus</Text>
    </View>
  );
};

export default revenus;

const styles = StyleSheet.create({});
